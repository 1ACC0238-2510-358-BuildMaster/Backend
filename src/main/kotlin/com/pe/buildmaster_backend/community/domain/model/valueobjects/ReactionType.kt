package com.pe.buildmaster_backend.community.domain.model.valueobjects

enum class ReactionType {
    LIKE,
    DISLIKE,
    REPOST // Podrías considerar si REPOST es una reacción o un tipo de Post diferente. Por ahora, lo mantenemos como reacción.
}